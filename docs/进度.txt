
Task 1: Maven 项目脚手架 (pom.xml, DdhAgentApplication, application.yml, schema.sql)

Task 2: 领域实体 — User, Project, ProjectTable, SourceTable, TableColumn

Task 3: 领域实体 — Conversation, Message, ConversationTable, EtlJob, EtlStep

Task 4: 领域 Repository 接口 + LlmPort

Task 5: MyBatis Plus Mapper 接口 (10个) + SourceTableMapper.xml

Task 6: Repository 实现 (5个 Impl)

Task 7: 基础设施配置类 (MybatisPlusConfig, CorsConfig, AppConfig)

Task 8: JWT 安全层 (JwtUtil, JwtAuthFilter, SecurityConfig) + TDD

Task 9: 公共 DTO + AppProperties

Task 10: Auth 模块 (AuthAppService + AuthController) + TDD

Task 11: Table 模块 (TableAppService + TableController) + TDD

Task 12: Project 模块 (ProjectAppService + ProjectController) + TDD

Task 13: Conversation CRUD 模块 (ConversationAppService + ConversationController) + TDD

Task 14: LlmConfig + ClaudeAdapter

Task 15: DeepSeekAdapter + QwenAdapter + OpenAiCompatibleAdapter 基类

Task 16: AgentDomainService (状态机 + 5工具调用循环)

Task 17: EtlDomainService + AgentAppService + GET /stream 端点

Task 18: JobAppService + JobController (list/get/sql/download-zip)

Task 19: AdminAppService + AdminController + DynamicLlmPort 热切换

Task 20: 集成测试 (AuthControllerTest + TableControllerTest + ConversationControllerTest + JobControllerTest)

Task 21: 最终验收 — 全部测试通过 + smoke test 清单

==========================================================
已实现至 Task 21（2026-06-14）。mvn test 全绿：12 tests, 0 failures。
为保证“前端零改动替换 Python 后端”，对计划做了以下兼容性修正：
- /admin/config 出入参为 {provider, model}（非计划的掩码 DTO），且不限管理员角色（对齐 Python get_current_user）。
- /jobs/{jobId}/steps/{stepId}/sql 返回 JSON {step_id, step_name, sql}（非纯文本），文件缺失返回 404。
- POST /projects/{id}/tables 返回 {associated: 数量}（对齐 Python AssociateResult）。
- SSE step_generated 事件携带 file 字段；终止发送 stream_end + end。
- EtlJobOut.target_schema 反序列化为 JSON 数组输出。
- Agent 在 state=2 收到新消息时按需求收集模式(state 1)重跑（对齐 Python）。
- generate_sql 工具内即写 SQL 文件（对齐 Python），AgentAppService 仅持久化 Job/Step。
注意：Java 8，不能用 Map.of / List.of。尚未 git commit（计划中的提交步骤待执行）。

