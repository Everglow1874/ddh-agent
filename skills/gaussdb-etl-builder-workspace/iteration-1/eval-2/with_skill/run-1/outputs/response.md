好的,我来帮你把销售明细表加工成一张「区域月度销售汇总表」,并生成可执行的 GaussDB ETL 作业写入输出目录。

不过在动手设计之前,我需要先把几样关键信息对齐——这是为了避免猜错字段、关联错表导致后面整套 SQL 白做。下面是我目前的理解和需要你补充的内容。

## 1. 需求复述

把**销售明细表(交易粒度的明细数据)**按**区域 + 月份**两个维度汇总,产出一张**区域月度销售汇总报表表**(预期会有销售额、订单数/笔数、可能的销量、客单价等指标)。最终生成完整的 ETL 作业(目标表 DDL + 清洗装载 SQL)并落盘到输出目录。

如果这个理解有偏差,请直接纠正我。

## 2. 需要你提供:源表结构(必需,我不会臆造)

生成 SQL 必须基于源表的**真实字段**。请把相关源表的建表语句或字段清单贴给我,包含:**表名、字段名、类型、注释**。我尤其需要确认:

- **销售明细表**的表名,以及其中:
  - **区域**信息从哪里来?是明细表里已有 `region` / `area` 字段,还是需要通过门店表 / 区域维表关联得到?如果需要关联,请一并提供维表结构和关联键。
  - **日期 / 时间**字段是哪一个(用于切分"月度"),类型是 `date` 还是 `timestamp`?
  - **销售金额、数量、订单号**等用于算指标的字段分别是哪些?

> 没有这些真实字段,我无法保证 SQL 可直接执行,所以这一步必须先拿到。

## 3. 可选:两类外挂文档(有就更好)

如果你那边已经沉淀了下面任一文档,请给我路径,我读取后能少问你很多问题、并按平台规范生成:

- **知识文档**:记录表用途、表间字段关联、1:1 / 1:N 基数等。有的话我就直接按里面已知的 join key 和基数来写,不再重复追问。
- **平台变量文档**:平台执行期支持的变量(如 `${TX_DATE}` 等)及其替换语义。如果这张汇总表是**每天 / 按月增量**跑的,我会用平台变量来写日期过滤,而不是写死日期。

两类文档都没有也能继续,只是少了"知识复用"和"执行期变量"的能力。另外也请告诉我:这张汇总表是**全量重算**还是**按月增量**装载?是否需要按天调度?

## 4. 输出目录

本次产物我会写入你指定的输出目录:
`D:\learning\project\pythonProject\ddh-agent\skills\gaussdb-etl-builder-workspace\iteration-1\eval-2\with_skill\outputs`

---

### 下一步

请把**销售明细表(及可能涉及的区域 / 门店维表)的字段清单**发我。拿到后我会:

1. **P1** 设计目标表「区域月度销售汇总表」的表名与字段(名称 / GaussDB 类型 / 注释),停下来等你确认;
2. **P2** 规划完整 ETL 步骤(建表、清洗装载,必要时才用临时表),再次等你确认;
3. **P3** 逐步生成每一步可直接执行的 GaussDB SQL,一步一个文件写入上面的输出目录,并附上 `plan.md`。

在拿到源表真实字段之前,我不会凭空编造表结构或 SQL——这是为确保最终作业能直接跑通。
