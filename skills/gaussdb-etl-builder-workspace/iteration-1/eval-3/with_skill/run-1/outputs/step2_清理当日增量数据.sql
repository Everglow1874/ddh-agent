-- ============================================================
-- Step 2: 清理当日增量数据(幂等)
-- T+1 增量作业:重跑前先删除目标表中 ${TX_DATE} 当天的汇总,
-- 避免重复执行导致数据翻倍。${TX_DATE} = 作业实例执行日的前一天。
-- ============================================================
DELETE FROM rpt_trade_daily_summary
WHERE stat_date = '${TX_DATE}';
